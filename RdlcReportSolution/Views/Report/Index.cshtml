@{
    ViewBag.Title = "Home Page";
}
<div class="jumbotron">
    <h1>ASP.NET</h1>
    <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
    <p><a href="http://asp.net" class="btn btn-primary btn-large">Learn more &raquo;</a></p>
</div>
<div class="row">
    <div class="col-xs-12 col-sm 12">
        <button class="btn btn-success partialClick" style="width:100%;text-align:center">click to load partial view</button>
    </div>
</div>
<hr />
<div class="row">
    <div class="col-md-12">
        <div class="jumbotron loadPartial">
            <form id="something">
                <input type="text" name="Id" id="Id" value="25"/>
                <input type="text" name="employeeName" id="employeeName" value="Mubusher"/>
                <input type="date" name="fromDate" id="fromDate" />
                <input type="date" name="toDate" id="toDate"/>
            </form>
            @*@Html.Action("ReportPartial")*@
        </div>
    </div>
</div>


@section scripts{
    @Scripts.Render("~/Scripts/pdfobject.min.js")
    <script type="text/javascript">
        $('.partialClick').on('click', function () {
            //var data = { Id: 25, name: "mubusher" };

            window.open('@Url.Action("VariationReport","foo")/?' + $('#something').serialize())
            @*$.ajax({
                url: '@Url.Action("DownloadFile")',
                // pass some parameters in data/params object
                type: 'GET',
                async: false,
                success: function (response) {
                    //$('.loadPartial').html(data);
                    if(response)
                    PDFObject.embed(response.fileUrl, ".loadPartial");
                },
                error: function (request, status, error) {
                    console.log(error);
                }
            });*@
        });
    </script>        
}